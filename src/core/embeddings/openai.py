"""OpenAI embeddings.

Nothing of our own lives here: LangChain's OpenAI embedder already fulfils the
contract, and the factory hands it out directly. Kept as the place a
hand-written OpenAI embedder would go if one is ever needed.
"""
